{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build Task",
            "type": "shell",
            "command": "cargo build",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "builds before it debugs"
        }
    ]
}
